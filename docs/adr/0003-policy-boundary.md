# ADR 0003: Effective decisions drive UX only

Status: accepted

The host injects an evaluated Effective Manifest. The UI applies explicit hide, disable, or read-only presentation. The backend or Go Proxy remains responsible for every authorization and for omitting unauthorized rows and fields.
