# Storybook and GitHub Pages

Run `npm run storybook` locally or `npm run build-storybook` for static output. Global toolbar controls switch theme, density, locale/direction, and Effective Manifest state. GitHub Pages deployment uses official artifact actions and derives the repository base path automatically.
